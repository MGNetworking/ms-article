version: '3.9'

services:
  article-service:
    container_name: article
    restart: always
    image: ms-article-service:${version:-latest}
    env_file:
      - .env
    build:
      context: ../
      dockerfile: dockerfile
      args:
        - CONFIG_SERVICE_URI_ARG=${IP_PROD}
        - env_profile=${env_PROD}
    deploy:
      replicas: 1
    ports:
      - "0:9010"
    volumes:
      # Path absolut pour l'échange des fichiers
      - ./$PWD TODO dossier local:/blog/images:rw
    environment:
      - SPRING_PROFILES_ACTIVE=${env_PROD}
      - CONFIG_SERVICE_URI=${IP_PROD}

    networks:
      - keycloak_postgre

networks:
  keycloak_postgre:
    name: sso_bd
    external: true  # indic qu'ils est déjà present
    driver: bridge  # Réseau docker isolé