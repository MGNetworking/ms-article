########## Localisation du fichier de configuration
# exemple : http://localhost:8089/msarticle/dev
spring:
  application:
    name: msarticle
  cloud:
    config:
      uri:
        - ${CONFIG_SERVICE_URI_host:http://ms-configuration:8089}